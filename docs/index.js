const basicInfo = require('./basicInfo');
const components = require('./components');
const components = require('./tasks');

module.exports = {
    ...basicInfo,
    ...components,
    ...tasks
    
};
